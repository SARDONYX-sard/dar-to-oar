# Dynamic Animation Replacer syntax

## Features

- [x] parser
- [ ] human readable error report

## Import rule

- No other `crate::` modules are called in this module to separate dependencies
  (it is OK to be called).
